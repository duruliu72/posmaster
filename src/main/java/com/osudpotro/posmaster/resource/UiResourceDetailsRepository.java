package com.osudpotro.posmaster.resource;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UiResourceDetailsRepository extends JpaRepository<UiResourceDetails,Long> {
}
